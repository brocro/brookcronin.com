{
  "name": "brookcronin",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:blog": "node scripts/build-blog.mjs",
    "build:experimental": "tsc && vite build",
    "build": "npm run build:blog && npm run build:experimental",
    "preview": "vite preview",
    "serve": "python3 -m http.server 8080 --directory dist"
  },
  "devDependencies": {
    "@types/three": "^0.168.0",
    "marked": "^18.0.4",
    "typescript": "^5.5.3",
    "vite": "^5.4.6"
  },
  "dependencies": {
    "@types/node": "^22.5.5",
    "postprocessing": "^6.36.2",
    "three": "^0.168.0"
  }
}
